import { describe, test } from "vitest";
import { setup, $fetch } from "@nuxt/test-utils";

describe("我的测试", async () => {
  await setup({
    // 测试上下文选项
  });

  test("我的测试", () => {
    // ...
  });
});
